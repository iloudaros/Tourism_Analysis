CREATE TABLE `Nights_spent` (
  `country` varchar(255),
  `residency` varchar(255),
  `number` float,
  `year` varchar(255)
);

CREATE TABLE `Arrivals` (
  `country` varchar(255),
  `residency` varchar(255),
  `number` float,
  `year` varchar(255)
);
