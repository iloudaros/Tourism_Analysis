CREATE TABLE `Nights_spent` (
  `country` varchar(255),
  `residency` varchar(255),
  `number` bigint,
  `year` int
);

CREATE TABLE `Arrivals` (
  `country` varchar(255),
  `residency` varchar(255),
  `number` bigint,
  `year` int
);
